let resultado;

const preco = 40;

if (preco < 40){
  resultado = "livro é barato.";
} else {
  resultado = "livro é não é barato."
}

console.log(resultado)

let x = 3;

console.log(x === 3);
console.log(x > 3);

let meuObjeto = {
  nome : 'Jeronimo Chiclana',
  idade : 20
}

console.log(meuObjeto)

const setinha =  (entrada) => {
  return entrada + 33;
}

const setinha2 = (entrada) => entrada + 34;

const meuArray = [21, 34, 'Julia', [32, 45]];

var minhaGlobal = 34;

function minhanovaFuncao(){
  var minhaGlobal = 23
  return minhaGlobal;
}

function minhaFuncaoMaior(entrada){
  let r;
  let resultado
  if (entrada % 2 === 0){
    r = 'par';
  }
  return r;
}

function minhaFuncaoMaior2(entrada){
  let r;
  if (entrada % 2 === 0) {
    r = 'par';
} else {
  r = 'impar';
}
  return r;
}

meuArray.forEach((el) => console.log(el))

const funcImprime = (ele, indice) => console.log("indice", indice, ":", ele)
meuArray.forEach(funcImprime)
